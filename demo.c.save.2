#include <stdlib.h>      // stdlib needed for getch
#include <ncurses.h>     // ncurses library
/* Internal function declarations   */
int food1 ();
int  snake ();           
int reload();

/* main function */

int main(void) {
  initscr();
  noecho();
  curs_set(FALSE);
 
  food1();
  snake();

  
  reload();

  

  endwin();
  return 0;
} 

/* internal functions  */
int reload()
{	
refresh();
  getch();
}

int  snake (){
int row = 10; int column = 33;
char body[] = "***";
  
for(column=33;column<200;column++)
  {
  mvaddstr(row, column,"*");
  reload();
  }
}

int  food1 (){
  mvaddstr(9, 32,"*");
}

